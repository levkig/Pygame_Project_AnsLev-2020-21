import pygame

pygame.init()
run = True
screen = pygame.display.set_mode((500, 500))
while run:
    for i in pygame.event.get():
        if i.type == pygame.QUIT:
            run = False
pygame.quit()
